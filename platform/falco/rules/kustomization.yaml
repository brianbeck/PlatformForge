# Additional Falco manifests deployed alongside the Helm chart.
# Argo CD syncs this directory as a separate source.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - prometheusrules.yaml
  - networkpolicy.yaml
