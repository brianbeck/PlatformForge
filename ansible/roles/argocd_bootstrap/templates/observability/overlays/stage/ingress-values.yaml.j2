# Generated by PlatformForge bootstrap -- Ingress disabled (using IngressRoutes)
# Grafana and Prometheus Ingress is handled via Traefik IngressRoute CRDs
grafana:
  ingress:
    enabled: false

prometheus:
  ingress:
    enabled: false
