# Loki datasource for Grafana — enables log queries in Grafana Explore.
# Generated by platformforge deploy.
grafana:
  additionalDataSources:
    - name: Loki
      type: loki
      access: proxy
      url: "{{ loki_url }}"
      isDefault: false
      editable: true
