# Trivy Operator prod overlay

# Standard 24h rescan in prod (from base values)
operator:
  vulnerabilityScannerScanInterval: 24h

# Larger resources for prod (more workloads to scan)
resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 512Mi
