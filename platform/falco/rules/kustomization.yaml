# This kustomization exists so Argo CD can deploy additional
# Falco rule ConfigMaps alongside the Helm chart.
# Add new rule files here as needed.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: []
# Add additional rule ConfigMaps here if needed beyond what
# the Helm chart's customRules handles. For most use cases,
# putting rules in base-values.yaml customRules is simpler.
